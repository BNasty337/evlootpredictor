{
  "name": "eatventure-tool",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@vercel/kv": "^3.0.0",
    "cookie": "^0.6.0",
    "node-fetch": "^2.6.7",
    "redis": "^5.8.2"
  },
  "devDependencies": {
    "javascript-obfuscator": "^4.1.0"
  },
  "scripts": {
    "obfuscate": "node obfuscate.js",
    "dev": "npx vercel dev",
    "build": "rm -rf public && mkdir -p public && npm run obfuscate && cp index.html unity-random.js public && cp -r src public"
  }
}
